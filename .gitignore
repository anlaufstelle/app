# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Coverage
htmlcov/
.coverage
coverage.xml
coverage.json

# Django
*.sqlite3
src/staticfiles/
src/static/css/styles.css
src/static/css/output.css
# Kompilierte Uebersetzungen: die getrackten src/locale/**/*.mo sind bewusst
# im Repo (Prod-i18n — der Docker-Build fuehrt KEIN compilemessages aus, die
# .mo werden per `COPY src/` ausgeliefert). Nur versehentliche Root-Streuner
# (z.B. ./messages.mo aus einem falsch aufgerufenen compilemessages) ignorieren.
/*.mo

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# AI Tools
CLAUDE.md
Gemini.md
.claude/*
!.claude/commands/
.codex
.codex/
.gemini/
.copilot/
.github/copilot/
.aider*
.cursor/
.continue/
.codeium/
.tabnine/
.superpowers/
docs/superpowers/
docs/plans/

# Media (encrypted file uploads)
src/media/

# OS
.DS_Store
Thumbs.db

# Environment & Secrets
.env
*.env.local
.env.dev
.env.deploy
*.pem
*.key
*.crt
*.p12
*.pfx

# Node (Tailwind)
node_modules/
# package-lock.json wird committet (Refs #656) — reproduzierbare
# `npm ci`-Installs + GitHub-Actions setup-node Cache-Hit.

# Backups
backups/

# SSL-Zertifikate (lokal generiert)
certs/

# Worktrees
.worktrees/

# Playwright MCP
.playwright-mcp/

# Screenshots (temporär)
admin-overview.png

# mutmut working tree
mutants/
.mutmut-cache*
# mutmut run-logs (Forensik nur, gehören nicht ins Repo)
mutmut-*.log
